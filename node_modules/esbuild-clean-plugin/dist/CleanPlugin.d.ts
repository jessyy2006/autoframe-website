import { BuildOptions, BuildResult } from 'esbuild';
export type PluginOptions = {
    dry?: boolean;
    initialCleanPatterns?: string[];
    verbose?: boolean;
};
export declare class CleanPlugin {
    private pluginOptions;
    private buildOptions;
    private previousAssets;
    constructor(pluginOptions: PluginOptions, buildOptions: BuildOptions);
    handleBuild(result: BuildResult): void;
    initialClean(): void;
    protected removeFiles(patterns: string[]): void;
    protected printStats(fileNames: string[]): void;
    validateOptions(): boolean;
}
